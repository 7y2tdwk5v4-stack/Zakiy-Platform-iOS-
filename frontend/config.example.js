// Copy this file to config.js and fill in your Supabase project's
// public values (Project Settings -> API in the Supabase dashboard).
// The anon key is safe to expose in client-side code.
window.LATE_CHAT_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-KEY",
  API_BASE: "", // leave empty when the frontend is served by the Flask backend
};
