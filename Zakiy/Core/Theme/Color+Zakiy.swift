import SwiftUI

/// Semantic color palette for ذكيّ, derived from the web platform's brand
/// colors (dark navy, mustard gold, teal) and re-expressed as light/dark
/// asset-catalog colors so the app follows the system appearance.
extension Color {
    static let zakiyNavy = Color("ZakiyNavy")
    static let zakiyBackground = Color("ZakiyBackground")
    static let zakiySurface = Color("ZakiySurface")
    static let zakiyMutedSurface = Color("ZakiyMutedSurface")
    static let zakiyTextPrimary = Color("ZakiyTextPrimary")
    static let zakiyTextSecondary = Color("ZakiyTextSecondary")
    static let zakiyMustard = Color("ZakiyMustard")
    static let zakiyTeal = Color("ZakiyTeal")
    static let zakiyBorder = Color("ZakiyBorder")
}
